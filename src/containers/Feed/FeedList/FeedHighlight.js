import React from 'react';

const FeedHighlight = (props) => (
    <div className="highLight_card">
        <h1>
            Rox last fed as: {props.}
        </h1>
    </div>
);

export default FeedHighlight;